using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SaveTest : MonoBehaviour
{
    public int level = 0;
    
    // Start is called before the first frame update
    void Start()
    {
        //level = SaveState.Instance.data.unlockedLevels;
    }
}
